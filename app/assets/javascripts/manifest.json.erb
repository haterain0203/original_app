<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Atopic Note",
  "short_name": "Atopic",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "assets/メモアイコン.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#000000",
  "background_color": "white",
  "display": "fullscreen",
  "orientation": "portrait"
}
