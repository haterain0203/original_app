<% provide(:title, "All users") %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
   <li><%= link_to user.name, user_path(user.id) %></li>
  <% end %>
</ul>

<div class="mt-5">
  <%= paginate @users %>
</div>